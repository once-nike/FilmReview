<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nike.douye.mapper.LoginMapper">
    <resultMap id="user" type="com.nike.douye.dto.UserDTO">
        <id column="id" property="id"></id>
        <result column="username" property="userName"></result>
        <result column="password" property="password"></result>
        <result column="email" property="email"></result>
        <result column="phone" property="phone"></result>
        <result column="address" property="address"></result>
    </resultMap>
   <!--根据用户名或手机号查询密码-->
    <select id="queryUserByUserNameOrPhone" resultMap="user">
        select id,username,password,email,phone,address from user_thing
        where username = #{userNameOrPhone} or phone = #{userNameOrPhone}
    </select>
    <!--获取uuid-->
    <select id="queryUuid" resultType="String">
        select uuid()
    </select>
    <!--将token添加入库-->
    <insert id="insertAccessToken">
        insert into access_token(user_id,access_token) values(#{userId},#{token})
    </insert>
    <!--删除数据库中的token记录-->
    <delete id="deleteAccessToken">
        delete from access_token where user_id = #{userId} and access_token = #{token}
    </delete>
</mapper>